name: Build Android APK

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.7'

      - name: Extract Android project
        run: |
          unzip -q MyanmarFortune_Android_MVP.zip -d source

      - name: Build APK
        run: |
          gradle -p source/MyanmarFortune assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: MyanmarFortune-APK
          path: source/MyanmarFortune/app/build/outputs/apk/debug/app-debug.apk
